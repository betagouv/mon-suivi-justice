<% content_for :content do %>
  <% flash.except(:timedout).each do |type, msg| %>
    <p class=<%= type %>><%= msg %></p>
  <% end %>

  <section class="authentication-form-container">
    <%= yield %>
  </section>
<% end %>

<%= render template: "layouts/application" %>
