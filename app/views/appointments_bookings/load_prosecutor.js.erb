try {
  appointment_type_container = document.getElementById('appointment-type-container');
} catch (error) {
  let appointment_type_container = document.getElementById('appointment-type-container');
}

appointment_type_container.insertAdjacentHTML('beforeend', "<%= j render partial: 'load_prosecutor', locals: {appointment_type: @appointment_type} %>");
