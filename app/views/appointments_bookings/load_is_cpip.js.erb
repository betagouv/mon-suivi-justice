document.getElementById('cpip-container').innerHTML = "<%= j render partial: 'load_is_cpip', locals: {current_user: current_user, convict: @convict} %>";
<% flash.each do |type, msg| %>
  $("#flash_messages").html("<div class=\"fr-alert <%= "fr-alert--#{type == 'notice' ? 'info' : type}" %> fr-alert--sm\"> <h3 class=\"fr-alert__title\">Attention : pas de commune renseignée</h3><p><%= msg %></p></div>")
<% end %>